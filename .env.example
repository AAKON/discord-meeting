DISCORD_TOKEN=
DISCORD_CLIENT_ID=
MONGODB_URI=
REDIS_URL=

# OpenAI — drives STT (Whisper) + summarize/tasks (GPT)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_STT_MODEL=whisper-1
OPENAI_STT_LANGUAGE=bn

# Transcription: openai | google | deepgram
TRANSCRIPTION_PROVIDER=openai

# Optional — only used if TRANSCRIPTION_PROVIDER switched to them
DEEPGRAM_API_KEY=
GOOGLE_API_KEY=
GOOGLE_STT_LANGUAGE=bn-BD
GROQ_API_KEY=
